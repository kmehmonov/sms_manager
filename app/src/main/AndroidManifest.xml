<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        SMS yuborish uchun zaruriy ruxsatlar.
        SEND_SMS — normal ruxsat, foydalanuvchi runtime'da tasdiqlashi shart.
        READ_PHONE_STATE — ko'p SIM kartali qurilmalarda SIM tanlash uchun.
    -->
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!--
        Faylni o'qish uchun ruxsat (Android 12 va undan pastda).
        Android 13+ da READ_MEDIA_* ruxsatlari kerak, lekin biz
        file picker ishlatganimiz uchun alohida ruxsat talab qilinmaydi.
    -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmsManager">

        <!-- Asosiy Activity — ilova ochilganda ko'rinadigan ekran -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
